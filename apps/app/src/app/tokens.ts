import { InjectionToken } from '@angular/core';

export const API_RTC_KEY = new InjectionToken('apiRTC Key')
export const STREAM_PUBLISH_DELAY = new InjectionToken('Publish delay')
export const STREAM_SWITCH_THRESHOLD = new InjectionToken('Stream switch threshold');
